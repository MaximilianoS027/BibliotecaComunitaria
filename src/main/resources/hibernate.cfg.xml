<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
    "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
    "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
  <session-factory>

    <!-- Conexión a PostgreSQL en Docker -->
    <property name="hibernate.connection.url">jdbc:postgresql://localhost:5434/biblioteca_bd</property>
    <property name="hibernate.connection.username">admin</property>
    <property name="hibernate.connection.password">admin</property>
    <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>
    <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>

    <!-- Configuración de logging SQL -->
    <property name="hibernate.show_sql">true</property>
    <property name="hibernate.format_sql">true</property>
    <property name="hibernate.use_sql_comments">true</property>

    <!-- Configuración de esquema -->
    <property name="hibernate.hbm2ddl.auto">update</property>

    <!-- Configuración de conexión para Hibernate 6 -->
    <property name="hibernate.connection.pool_size">10</property>
    <property name="hibernate.current_session_context_class">thread</property>
    <property name="hibernate.jdbc.batch_size">20</property>
    
    <!-- Configuración específica para Hibernate 6 -->
    <property name="hibernate.connection.provider_disables_autocommit">true</property>
    <property name="hibernate.jdbc.time_zone">UTC</property>

    <!-- Mapeos de las clases de entidad -->
    <mapping class="logica.Lector"/>
    <mapping class="logica.Bibliotecario"/>
    <mapping class="logica.Usuario"/>
    <mapping class="logica.Zona"/>
    <mapping class="logica.EstadoLector"/>
    <mapping class="logica.Material"/>
    <mapping class="logica.ArticuloEspecial"/>
    <mapping class="logica.Libro"/>
    <mapping class="logica.Prestamo"/>
    <mapping class="logica.EstadoPrestamo"/>

  </session-factory>
</hibernate-configuration>